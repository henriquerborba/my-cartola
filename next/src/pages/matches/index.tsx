import { Button } from "@mui/material";
import { NextPage } from "next";

const ListMatchsPage: NextPage = () => {
    return (
        <div>
            matches
            <Button variant="contained">Teste</Button>
        </div>
    );
};

export default ListMatchsPage;
